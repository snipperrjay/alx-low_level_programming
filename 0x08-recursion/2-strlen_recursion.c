#include "main.h"
#include <string.h>

/**
 * main.h - check code
 *
 * _strlen_recursion - print length of string
 *
 * @*s: string
 * Return : nothing
 **/

int _strlen_recursion(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i);

	_strlen_recursion
	return 0;
}
